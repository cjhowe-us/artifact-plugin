# document-confluence storage

Stores `document` artifacts as Confluence Cloud pages via the REST v2 API.

URI: `document|document-confluence/<space>/<page-id>`

Requires env: `CONFLUENCE_BASE_URL`, `CONFLUENCE_USER`, `CONFLUENCE_TOKEN`.
