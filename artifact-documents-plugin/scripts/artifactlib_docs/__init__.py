"""artifact-documents-plugin package marker.

Ships schemes, storages, and templates consumed by the artifact-plugin
Claude Code runtime. The runtime loads them from filesystem paths, so the
Python-importable surface is intentionally minimal.
"""

__version__ = "2.0.2"
