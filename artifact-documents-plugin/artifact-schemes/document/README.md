# document

Vertex scheme. Markdown body + structured content (title, authors, status).

URI: `document|file/<id>` (e.g. `document|file/docs/design/auth`). Stored as `<id>.md` + `<id>.content.toml` by the
`file` storage.
